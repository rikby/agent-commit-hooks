# Block absolute home paths in staged code files
# P0 — prevents /Users/kirby/... leaks
#
# Uses git diff --cached directly (no file args needed).

pre-commit:
  scripts:
    "block-home-paths-code.sh":
      runner: sh
